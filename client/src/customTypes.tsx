export type NavItems = {
    name: string;
    href: string,
    active: boolean,
}

export type SparkCardProps = {
    title: string,
    description: string,
}
export type SparkButtonProps = {
    btnName: string,
}